# Contributing

Keep the project local-first and independently maintainable. Do not add code, fixtures, operational details, credentials, product copy, or deployment configuration from private systems.

Test public behavior through the local extraction contract. Fixtures must be invented, public-domain, or otherwise demonstrably redistributable.

Before submitting a change, run:

```sh
pnpm test
pnpm typecheck
```
